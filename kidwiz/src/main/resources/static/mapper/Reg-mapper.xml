<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kidwiz.web.DAO.RegDAO">
	<update id="accept" parameterType="String">
	UPDATE reg_consulting SET stat = '1' WHERE reg_no = #{reg_no} 
	</update>
	
	<insert id="regconInsert" parameterType="map">
	INSERT INTO reg_consulting (stuNum) VALUES (#{stuNum})
	</insert>
	
	<select id="regDetail" parameterType="map" resultType="map">
	SELECT ROW_NUMBER() OVER (ORDER BY DATE) as rownum, stuNum, DATE, TIME, reg_no FROM timeTestReg WHERE reg_no=#{rgno}
	</select>
	
	<select id="timetable" resultType="map">
	select * from timeTest
	</select>
	
	<insert id="regSubmit" parameterType="map">
	INSERT INTO timeTestReg (stuNum, date, time, reg_no, memo) VALUES (#{stuNum}, #{date}, #{time}, #{regno}, #{memo})
	</insert>
</mapper>